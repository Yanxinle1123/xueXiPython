import tkinter

window = tkinter.Tk()
window.width = 800  # 设置窗口的宽
window.height = 600  # 设置窗口的高
window.title('Hello Tkinter')  # 设置窗口的标题
window.resizable(False, False)  # 设置窗口的大小为不可调整
window.mainloop()  # 显示窗口
